# Sliding window extraction and tensor construction — Module 6 implementation.
#
# Planned:
#   create_windows(episode, window_size, stride) -> list of (window, label)
#   episodes_to_tensors(episodes, ...) -> (X, y) numpy arrays
#   split_dataset(X, y, ...) -> train/val/test splits
